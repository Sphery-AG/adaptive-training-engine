node_modules/
.next/
__pycache__/
*.pyc
.env
.env.local
venv/
.DS_Store
*.sql
*.db
.gstack/

# Local-only, confidential reference material — NEVER commit.
# Conductor workspaces get _local/ via symlink (scripts/conductor-setup.sh),
# so it stays a single uncommitted copy in the root clone.
/_local/
.venv/
